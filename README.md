Inject Header
=============

A simple browser extension for adding additional headers sent with every HTTP-request.
Supports both Chrome and Firefox.

Build
-----

Build a zip package by running the following, 
or an equivalent command for the package manager you're using.

```shell
npm run package
```

The built zip-file can be found in the web-ext-artifacts directory.

